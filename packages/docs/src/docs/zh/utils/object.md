---
title: 对象工具
source: packages/abm-utils/src/object.ts
---

```ts
import 'abm-utils/object';
```

# 泛型 `Constructor`
构造函数类型。

# 函数 `notNil`
判断值是否非空（非 `null` 且非 `undefined`）。

# 函数 `normalizeError`
将输入的参数标准化为 `Error` 对象。

# 函数 `proxyObject`
创建一个代理对象，该对象在属性被设置或删除时调用指定的更新函数。
