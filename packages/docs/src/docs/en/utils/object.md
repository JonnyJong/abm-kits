---
title: Object
source: packages/abm-utils/src/object.ts
---

```ts
import 'abm-utils/object';
```

# Generic `Constructor`
Constructor function type.

# Function `notNil`
Checks if a value is not null (neither `null` nor `undefined`).

# Function `normalizeError`
Normalizes the input parameter to an `Error` object.

# Function `proxyObject`
Creates a proxy object that calls the specified update function when properties are set or deleted.