:host
	display inline-flex
	justify-content center
	align-items center
	padding 0 6px
	min-width 28px
	height 28px
	border 1px solid var(--w-hint-key-border)
	border-radius 4px
	background var(--w-hint-key-bg)
	white-space pre
	font-size 14px
